# Archived skills and agents. This directory sits outside skills/ and agents/ so no discovery mechanism, current or future, can load it.
To restore: `git mv archive/skills/<name> skills/<name>` (or `git mv archive/agents/<name>.md agents/<name>.md`) — nothing else needed.
Archived 2026-07-10 per docs/audits/context-audit-2026-07-10.md (SPECULATIVE tier, the four skills merged into ui-quality, and two redundant agents).
